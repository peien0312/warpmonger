# Pinned to the versions actually running on the production VM (tested in prod),
# so a venv rebuild reproduces prod and `pip install -r` never downgrades it.
Flask==3.1.3
Werkzeug==3.1.8
Pillow==12.3.0
Markdown==3.10.2
python-dotenv==1.2.2
cryptography==49.0.0
gunicorn==26.0.0
sentry-sdk==2.18.0
