Flask==3.1.2
Werkzeug==3.1.3
Pillow==12.0.0
Markdown==3.10
python-dotenv==1.2.1
cryptography==49.0.0
sentry-sdk==2.18.0
# Not in the local venv (installed only on the VM); pin a recent stable release.
gunicorn==23.0.0
